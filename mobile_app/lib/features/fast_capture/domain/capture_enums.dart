enum CaptureEventType {
  meeting,
  workout,
  transit,
  meal,
  generic,
}

enum TimeAmbiguityKind {
  none,
  amPmHour,
  missingTime,
}

enum TimePeriod {
  morning,
  afternoon,
  evening,
  allDay,
}
